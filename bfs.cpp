#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

/* 문제 정의
 ->
 */

/* 해결 알고리즘 생각

*/

/*cycle이 존재할 경우 visited가 필요! -graph search와 tree search의 차이점. 근데 tree도 노드가 양항향 연결되어 있다고 보면 visited 필요하지 않나?
visit 체크/입력 방식?
큐에 넣기 전 입력: 방문될 예정이므로!
방문했을 때 입력: state search에서 필요한 경우가 있을수도..
*/

void bfs()
{
    int
}

int main()
{
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);

    return 0;
}